version: "2"

run:
  timeout: 5m
  exclude-dirs:
    - testdata

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple

formatters:
  enable:
    - gofmt
